local ran_ok, error = pcall(function() local ran_ok, kpse = pcall(require, "kpse") if ran_ok then kpse.set_program_name("luatex") end local lfs = require("lfs") local cacheDir = "./_markdown_scriptbook" if not lfs.isdir(cacheDir) then assert(lfs.mkdir(cacheDir)) end local md = require("markdown") local convert = md.new({cacheDir = "./_markdown_scriptbook", frozenCacheFileName = "./_markdown_scriptbook/frozenCache.tex", frozenCacheCounter = 0, } ) local file = assert(io.open("1-plantar-cara/00-prologue.md", "r"), [[could not open file "1-plantar-cara/00-prologue.md" for reading]]) local input = assert(file:read("*a")) assert(file:close()) print(convert(input:gsub("\r\n?", "\n") .. "\n")) end) if not ran_ok then local file = io.open("./scriptbook.markdown.err", "w") if file then file:write(error .. "\n") file:close() end print('\\markdownError{An error was encountered while executing Lua code}{For further clues, examine the file "./scriptbook.markdown.err"}') end
